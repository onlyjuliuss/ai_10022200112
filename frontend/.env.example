# Backend API origin (no trailing slash). Built into the app at compile time.
# Leave empty when the frontend is served from the same host as the API (FastAPI static files).
# Local dev: leave empty to use Vite proxy, or set e.g. http://localhost:8000
VITE_API_BASE_URL=
